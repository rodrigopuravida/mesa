<h2>Mesa Chefs</h2>

<% chefs.forEach(function(chef) { %>
<div class='chefwell well'>
        <img id='chefphoto' src=<%= chef.photo%>>
        <h2><a href='/chefs/<%= chef.id %>/show'><%= chef.user.name %></a></h2>
        <h2><%= chef.restaurant %></h2>

</div>
<% }); %>